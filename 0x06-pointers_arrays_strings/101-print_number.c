#include "main.h"
/**
 * print_number - a function that prints an integer
 * @n: input number
 * Return: 0 Always succesful
 */

void print_number(int n);
{
	int n;

	if (n <  0)
	{
		_putchar('-');
		_putchar("%d\n", n);
	}
	else if (n < 0)
		_putchar("%d\n", n);
	else
		_putchar("%\n", '0');
	_putchar('\n');

	return (0);
}
