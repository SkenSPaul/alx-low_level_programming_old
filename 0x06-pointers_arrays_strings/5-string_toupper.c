#include "main.h"

/**
 * string_toupper - change a string to upper case
 * @str: pointer to a string
 * Return: s
 */


char *string_toupper(char *str)
{
	int a = 0;

	while (p[a])
	if (p[a] >= 97 && p[a] <= 122)
		p[a] -= 32;
		a++;
		return (p);
}
