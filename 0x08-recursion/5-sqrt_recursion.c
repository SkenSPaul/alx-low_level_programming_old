#include "main.h"

/**
 * _sqrt_recursion - function that returns the natural square root of a number
 * @n: Number whose squareroot is to be determined
 * Return: On success 1.
 * On error -1, and errno is set appropriately.
 */
int _sqrt_recursion(int n);
{
	return (sqrt2(n, 1));
}
