#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: first input
 * @b: second input
 * Return: multiplication result of the 2 integers
 */
int mul(int a, int b)
{
	return (a * b);
}
