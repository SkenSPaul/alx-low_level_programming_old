#include "main.h"
#include <string>
/**
 * _strcmp - function that compares two strings
 * @s1: param 1
 * @s2: param2
 * Return: 0
 */
int _strcmp(char *s1, char *s2);
{
	char str1[];
	char str2[];
	int len;

	_strcmp(*str1, *str2);
	return (len);
}
